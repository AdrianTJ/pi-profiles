name: ci

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Same script the pre-push hook runs, so a local pass means a green run.
      - run: ./check.sh
